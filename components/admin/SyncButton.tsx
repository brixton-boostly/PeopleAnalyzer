'use client'

// Gusto sync is handled by Claude via MCP — this button is a placeholder
// for a future direct API integration. Ask Claude to run sync-gusto-mcp.mjs
// whenever you need to refresh employee/manager data for a cycle.
export function SyncButton({ cycleId: _ }: { cycleId: string }) {
  return null
}
